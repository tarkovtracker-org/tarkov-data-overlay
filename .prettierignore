node_modules/
dist/
package-lock.json

# Machine-generated with a byte-for-byte locked format (regenerated by the
# eft:story pipeline and guarded by tests/eft-story.test.ts). Prettier must not
# touch it, or the round-trip test breaks.
src/additions/storyChapters.json5
