{
  "$id": "task-divergence.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": {
    "additionalProperties": false,
    "properties": {
      "fields": {
        "additionalProperties": {
          "additionalProperties": false,
          "properties": {
            "confidence": {
              "enum": ["high", "medium", "low"]
            },
            "note": {
              "type": "string"
            },
            "pve": {
              "type": ["number", "string", "boolean", "null"]
            },
            "pveSource": {
              "enum": ["wiki", "eft-reference", "tarkov.dev", "in-game"]
            },
            "regular": {
              "type": ["number", "string", "boolean", "null"]
            },
            "regularSource": {
              "enum": ["wiki", "eft-reference", "tarkov.dev", "in-game"]
            },
            "verified": {
              "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
              "type": "string"
            }
          },
          "required": ["confidence", "verified"],
          "anyOf": [
            { "required": ["regular"] },
            { "required": ["pve"] }
          ],
          "type": "object"
        },
        "minProperties": 1,
        "type": "object"
      },
      "name": {
        "type": "string"
      },
      "note": {
        "type": "string"
      },
      "proof": {
        "format": "uri",
        "type": "string"
      },
      "status": {
        "enum": ["divergent", "converged", "mode-exclusive"]
      }
    },
    "required": ["name", "proof", "status", "fields"],
    "type": "object"
  },
  "type": "object"
}
